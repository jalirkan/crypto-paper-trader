# Bitcoin Core — signet, pruned. RPC password injected by install.sh.
signet=1
server=1
prune=2000
txindex=0
rpcuser=cpt
rpcpassword=__RPC_PASS__

[signet]
rpcbind=127.0.0.1
rpcallowip=127.0.0.1
zmqpubrawblock=tcp://127.0.0.1:28332
zmqpubrawtx=tcp://127.0.0.1:28333

# --- mainnet flip (later): comment out signet=1 above, and move the
# [signet] section contents under a [main] section instead.
